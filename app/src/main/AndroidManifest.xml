<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roufy235.travel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Controllers.WelcomeActivity"
            android:theme="@style/noActionBarWithBlue">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Controllers.DecisionActivity"
            android:theme="@style/noActionBar" />
        <activity
            android:name=".Controllers.SignInActivity"
            android:theme="@style/noActionBarWithBlue" />
        <activity
            android:name=".Controllers.SignUpActivity"
            android:theme="@style/noActionBarWithBlue" />
        <activity
            android:name=".Controllers.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/noActionBarWithBlue"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".Controllers.PlaneActivity"
            android:theme="@style/noActionBarWithBlue" />
        <activity
            android:name=".Controllers.FlightsResultActivity"
            android:theme="@style/noActionBarWithBlue" />
        <activity android:name=".Controllers.FlightDetailsActivity"
            android:theme="@style/noActionBarWithBlue"></activity>
    </application>

</manifest>